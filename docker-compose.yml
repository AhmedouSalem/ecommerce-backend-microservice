services:
  gateway-service:
    #    build: ./gateway-service
    #    image: gateway-service:v1.0.0
    image: ecom-gateway-service:latest
    container_name: gateway-service
    ports:
      - "8891:8891"
    networks:
      - ecom-network

networks:
  ecom-network:
    external: true
