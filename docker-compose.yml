services:
  config-service:
    #    build: ./config-service
    #    image: config-service:v1.0.0
    image: ecom-config-service:latest
    container_name: config-service
    ports:
      - "9999:9999"
    networks:
      - ecom-network

networks:
  ecom-network:
    external: true
